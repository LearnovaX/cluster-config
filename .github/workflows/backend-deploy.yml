name: Backend Infrastructure Pipeline

on:
  push:
    branches:
      - main

    paths:
      - 'backend/prod/kustomization.yml'
    
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Render kustomize locally (no cluster access)
      - name: Render manifests (kustomize)
        run: |
          kubectl kustomize backend/prod > /tmp/manifests.yaml

      - name: Install kubeconform
        run: |
          curl -sSL https://github.com/yannh/kubeconform/releases/latest/download/kubeconform-linux-amd64.tar.gz \
            | tar -xz -C /usr/local/bin kubeconform
          kubeconform -v

      - name: Validate manifests (kubeconform)
        run: |
          # -strict: catch missing required fields
          # -ignore-missing-schemas: don’t fail on CRDs/custom resources unless you provide schemas for them
          kubeconform -strict -ignore-missing-schemas -summary /tmp/manifests.yaml


  deploy:
    environment: prod
    needs: validate

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" > ~/.kube/config

      - name: Show diff
        run: |
          kubectl diff -k backend/prod || true

      - name: Render secrets
        env:
          DJANGO_ENV: ${{ vars.DJANGO_ENV }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          DEBUG: ${{ vars.DEBUG }}

          POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_HOST: ${{ secrets.POSTGRES_HOST }}
          POSTGRES_PORT: ${{ secrets.POSTGRES_PORT }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

          REDIS_HOST: ${{ secrets.REDIS_HOST }}
          REDIS_PORT: ${{ secrets.REDIS_PORT }}

          EMAIL_BACKEND: ${{ secrets.EMAIL_BACKEND }}
          EMAIL_HOST: ${{ secrets.EMAIL_HOST }}
          EMAIL_PORT: ${{ secrets.EMAIL_PORT }}
          EMAIL_USE_TLS: ${{ secrets.EMAIL_USE_TLS }}
          EMAIL_HOST_USER: ${{ secrets.EMAIL_HOST_USER }}
          EMAIL_HOST_PASSWORD: ${{ secrets.EMAIL_HOST_PASSWORD }}

          FRONTEND_URL: ${{ vars.FRONTEND_URL }}
          
          DOMAIN_URL: ${{ vars.DOMAIN_URL }}
          ENROL_LINK_URL: ${{ vars.ENROL_LINK_URL }}

          USE_S3: ${{ vars.USE_S3 }}
          USE_S3_FOR_STATIC: ${{ vars.USE_S3_FOR_STATIC }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_STORAGE_BUCKET_NAME: ${{ secrets.AWS_STORAGE_BUCKET_NAME }}
          AWS_S3_REGION_NAME: ${{ secrets.AWS_S3_REGION_NAME }}

          FILE_LOGGING_ENABLED: ${{ vars.FILE_LOGGING_ENABLED }}

        run: |
          cp -r backend/prod /tmp/backend-prod

          find /tmp/backend-prod -type f -name "*.yml" -exec sh -c '
            envsubst < "$1" > "$1.tmp" && mv "$1.tmp" "$1"
          ' _ {} \;

      - name: Deploy backend stack
        run: |
          kubectl apply -k /tmp/backend-prod

      - name: Verify rollout
        run: |
          kubectl rollout status deployment/lms-backend-deployment -n lms --timeout=120s
